apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "trading-floor.fullname" . }}-config
  labels:
    {{- include "trading-floor.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
